// Project configuration
brick.pref {
    // Brick connection parameters //
    sshHost = "10.0.1.1"
    sshUser = "robot"
    sshPassword = "maker"

    // Do not use stuff embedded in manifest (for easier main class changes)
    useEmbeddedPaths = false
}

brick.paths {
    wrapperDir = "/home/robot/ali-idrissou-plong-2020"
}
